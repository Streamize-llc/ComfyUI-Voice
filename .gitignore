# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Per-engine isolated venvs + cloned upstream inference repos (set up locally;
# never committed). Keep runtimes/worker.py (the only tracked file in runtimes/).
comfyui_voice/runtimes/*/
comfyui_voice/runtimes/*.log
runtimes/*/
*.log

# Model weights / caches / outputs
models/
# ...but the vendored MOSS package ships a diffsynth/models/ *source* dir.
!comfyui_voice/_vendor/**/models/
output/
*.wav
*.flac
*.mp3
*.npy
*.safetensors
*.pth
*.ckpt

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
.tmp_moss/
